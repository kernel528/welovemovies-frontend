services:
  - type: web
    runtime: static
    name: kernel528-welovemovies-dashboard
    branch: main
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
